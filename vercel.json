{
    "rewrites": [
        { "source": "/events/(.*)", "destination": "/events" },
        { "source": "/api/(.*)", "destination": "/api" },
        { "source": "/(.*)", "destination": "/" }
    ]
}
